name: CI
on: 
  push:
    paths:
      - LICENSE.md 

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Set up Python 
      uses: actions/setup-python@v1
      with:
        python-version: '3.11'
    - name: Install dependencies
      run: |
        pip install setuptools wheel&& sudo rm -rf /usr/local/lib/android /opt/hostedtoolcache/CodeQL
        python src/test.py 
